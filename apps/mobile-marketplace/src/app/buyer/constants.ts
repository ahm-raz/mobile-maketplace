/** Rows shown on `/buyer` before "See more" (full history on `/buyer/viewed`). */
export const BUYER_HOME_RECENT_VIEWS_LIMIT = 10;

/** Page size for `/buyer/viewed` (API max 50 per request). */
export const BUYER_VIEWED_HISTORY_PAGE_LIMIT = 50;
